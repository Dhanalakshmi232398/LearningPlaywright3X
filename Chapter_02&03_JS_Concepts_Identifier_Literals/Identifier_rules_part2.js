var name = "Rithan";

var firstName = "DhanaMathu"; // This is valid and recommended to use camelCase for variable names
var This_is_a_variable = "hello"; // This is valid, but not recommended as it can be confusing
var lastName = "hello"; // This is valid and recommended to use camelCase for variable names

// Naming conventions (cases)
//----------------------------------
//1. camelCase( standard for JS variables and functions)
let userName = "camelCase";
let userAge = 25;
let totalAmount = 1000.00;
let isAvailable = true;
function calculateTotalAmount(price, quantity) {"return function camel case";}

//2. PascalCase (standard for JS classes and constructor functions)
let UserDetails = "PascalCase";
let ShoppingCart = "cart";
function Amount(price, quantity) {"return constructor";}


//3. snake_case (underscore seperated words)
let user_details = "snake_case";
let user_age = 25;
let is_available = true;

//4. screaming_snake_case (Constatnts, uppercase underscore separated words)
const MAX_USERS = 100;
const DEFAULT_TIMEOUT = 5000;
const API_URL = "https://api.example.com";


//5. hungarianNotation (prefixes to indicate the type of variable - older style)
let strUserName = "HungarianNotation"; // str for string
let intUserAge = 25;    
let boolIsAvailable = true; // bool for boolean 
let nCount = 10; // n for number
let arrItems = [1, 2, 3]; // arr for array

